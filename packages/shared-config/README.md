# packages/shared-config

Configuration et feature flags partagés entre les apps (règles de lint/format communes, constantes de configuration non sensibles, définition des feature flags par tenant évoqués en section 4 du cahier des charges).
