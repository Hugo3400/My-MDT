# packages/shared-types

Types TypeScript partagés entre `apps/web` et `apps/api` (contrats API, événements Socket.IO, énumérations métier — statuts, priorités, catégories). Évite la duplication et les divergences de contrat entre le panel et le serveur.
